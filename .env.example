# ── LLM provider keys (at least one required) ────────────────────────
OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=...
# OPENROUTER_API_KEY=sk-or-...

# ── Models (required) ────────────────────────────────────────────────
# Built-in: claude-opus-4-6, claude-sonnet-4-6, gpt-5.2, gemini-3-pro-preview, gemini-3-flash-preview
# Custom models can be added via ~/.ntrp/models.json (see docs/setup.md)
NTRP_CHAT_MODEL=claude-sonnet-4-6
# Memory consolidation (can be cheaper/faster)
NTRP_MEMORY_MODEL=gemini-3-flash-preview
# Explore agent (default: same as chat model)
# NTRP_EXPLORE_MODEL=gemini-3-flash-preview
# Embeddings: text-embedding-3-small, text-embedding-3-large, gemini-embedding-001
NTRP_EMBEDDING_MODEL=text-embedding-3-small

# ── Optional integrations ────────────────────────────────────────────
# Exa.ai web search
# EXA_API_KEY=...

# Telegram notifications (see docs/setup.md)
# TELEGRAM_BOT_TOKEN=...

# Obsidian vault path
# NTRP_VAULT_PATH=/path/to/your/vault

# Browser history (chrome, safari, arc)
# NTRP_BROWSER=chrome
# NTRP_BROWSER_DAYS=30

# Gmail & Calendar (requires Google OAuth setup, see docs/setup.md)
# NTRP_GMAIL=true
# NTRP_CALENDAR=true

# ── API auth (required) ───────────────────────────────────────────────
NTRP_API_KEY=your-secret-key
